<div class="container client-directory mt-5">

	<h1 class="text-center mb-5 ">Client Directory</h1>

		<div class="row">

			<% @project_members.map(&:client).uniq.each do |pm| %>
				<% if pm != nil %>
					<div class="col-md-4 mb-4">

						<div class="card">
							<div class="card-header"><h4 class="card-title"><b><%= pm.company_name %></b></h4>

								<% if pm.profile_photo.url == nil %>
						          <img class="rounded-circle" alt="100x100" width="150" height="150" src="https://wedevelop.s3-us-west-2.amazonaws.com/computer-1331579_640.png" data-holder-rendered="true">
						        <% else %>
						          <%= image_tag(pm.profile_photo.url(:client_card), class: 'rounded-circle') %>
						        <% end %>

							</div>

								<div class="card-body">

									<p>Contact: <b><%= pm.first_name %> <%= pm.last_name %></b></p>
									<p>Phone: <b><%= pm.phone %></b></p>
									<p>Email: <b><%= pm.email %></b></p>
									<p>Rate: <b>$<%= pm.rate %>/hr</b></p>
									<p>Address: <b><%= pm.street_address %></b></p>
									<p><b><%= pm.city %></b>, <b><%= pm.state %></b>, <b><%= pm.zipcode %></b></p>
									

								</div>


						</div>

					</div>				
				<% end %>

			<% end %>

		</div>

</div>